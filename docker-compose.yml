version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - audio-network

  broker:
    build: ./audio-broker-service
    ports:
      - "5000:5000"
    environment:
      - REDIS_HOST=redis
    depends_on:
      - redis
    networks:
      - audio-network

  worker:
    build: ./audio-worker-service
    environment:
      - REDIS_HOST=redis
    depends_on:
      - redis
    networks:
      - audio-network

  frontend:
    build: ./frontend-service
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:5000
    depends_on:
      - broker
    networks:
      - audio-network

networks:
  audio-network:
    driver: bridge